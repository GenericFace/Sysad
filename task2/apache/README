BUILD COMMAND:

docker build -t crazy .

RUN COMMAND:

docker run -it --name gammaz -p 80:80 --rm crazy

If the gammaz.hm.com isnt working and its only serving on localhost, then 

change "localhost" to "gammaz.hm.com" in /etc/hosts using the vi editor 

If apache doesnt run, run the command 

docker -it exec gammaz bash
